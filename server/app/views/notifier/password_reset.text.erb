Hi there,

So you've forgotten your password at Nanospace? No problem! Just visit the following link to reset your password:

<%= "#{SITE_URL}/#t=#{@user.id},#{@user.token}" %>

(If you didn't request a password reset, just ignore this email)
